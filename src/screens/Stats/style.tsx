/* stats.tsx screen style */
import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  pieAndTagsContainer: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
  },
});

export default style;
