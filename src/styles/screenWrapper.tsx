/* screenWrapper.tsx style */
import { StyleSheet } from "react-native";

const screenWrapper = StyleSheet.create({
  style: {
    flex: 1,
    backgroundColor: "#ffffff",
    color: "#000000",
    paddingHorizontal: 25,
    paddingTop: 50,
  },
});

export default screenWrapper;
