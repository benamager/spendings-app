/* addexpense.tsx component style */
import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  container: {
    position: "absolute",
    backgroundColor: "red",
    bottom: 0,
    right: 0,
    left: 0,
    zIndex: 10,
  },
});

export default style;
